// STUB — port from ./NotificationsBell.source.tsx during Phase 1 refactor.
// See README.md "Phase 1 refactor checklist" for the exact transformations.
export function NotificationsBell(_props: Record<string, unknown> = {}) {
  throw new Error(
    "@joasuite/shared-ui: NotificationsBell is not yet ported. See README.md.",
  );
}
