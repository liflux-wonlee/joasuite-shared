# JoaSuite core-vendor manifest
#
# Each line is a file path relative to an app repo's root. These files are
# vendored (copied), NOT imported as part of the @joasuite/shared-ui package
# in ../src — they don't fit the dependency-injection package pattern
# (either because they're plain utilities with zero app-specific coupling,
# where a real package export is future work, or because they're domain
# business logic too tightly coupled to one app's bills/payment_requests
# tables to cleanly factor into a DI-injectable server function yet).
#
# Canonical source for every file below is currently JoaBooks. Do not edit
# these directly inside an app repo — edit the canonical app, push here,
# then pull into every other app. See README.md for the upstream-fix
# workflow when a non-canonical app genuinely needs a change.
#
# Lines starting with # and blank lines are ignored.

# Plain utilities (no app-specific dependencies) — candidates to eventually
# become real @joasuite/shared-ui package exports instead of copy-vendoring.
src/lib/timezone.ts
src/lib/format.ts
src/lib/utils.ts
src/lib/error-capture.ts
src/lib/error-page.ts
src/hooks/use-mobile.tsx
src/hooks/useIsTenantAdmin.ts
src/integrations/supabase/auth-attacher.ts
src/integrations/supabase/auth-middleware.ts
src/lib/cron-auth.server.ts

# Domain business logic (recurring / "Planned Transactions") — reconciled
# 2026-07-06, re-confirmed 2026-07-07, JoaBooks version adopted as canonical
# for both JoaBooks and JoaOffice. Required post-pull edits per app (do NOT
# skip these — a mechanical pull-from-core without them reintroduces real
# bugs, e.g. Cash Flow gated on the wrong app's subscription):
#
#   recurring-v2.functions.ts
#     - Change the single `const APP_CODE = "joabooks"` line to "joaoffice"
#       (and vice versa). Everything else must stay byte-for-byte identical.
#
#   RecurringForm.tsx
#     - JoaBooks-only imports with no JoaOffice equivalent:
#       `listAccountsWithBalance` (@/lib/accounting.functions) → JoaOffice
#       has `listPaymentAccounts` (@/lib/admin.functions) instead, which
#       returns the rows array directly (not `{ rows: [...] }`) — adjust
#       the accessor accordingly.
#       `AddVendorDialog` (@/components/AddVendorDialog) has no JoaOffice
#       equivalent — drop the inline "+ New Party" button/dialog wiring in
#       JoaOffice's copy; the party dropdown itself still works.
#     - Every other app-specific route/table this file might reference
#       (parties, currencies) already exists with matching signatures in
#       both apps as of 2026-07-07 — re-verify on future pulls in case that
#       changes.
#
# See docs/prompts/joaoffice-core-migration/08-recurring-transactions.md
# and 09-core-resync-2026-07.md in the joabooks repo for the full spec this
# module was ported against.
src/lib/recurring-v2.functions.ts
src/lib/recurring-preview.ts
src/components/recurring/RecurringForm.tsx

# Cash Flow Forecast page — reconciled 2026-07-11. Confirmed near-byte-
# identical between JoaBooks and JoaOffice already (only whitespace/comment
# noise + a `p-8` wrapper class in JoaOffice's copy). No documented
# post-pull edits needed beyond that wrapper class, if present.
src/routes/app.cashflow.tsx

# "New Planned Transaction" page — reconciled 2026-07-11. Required
# post-pull edits per app:
#   - Back link: JoaBooks uses an inline `Link` + `ArrowLeft`; JoaOffice
#     uses its own `BackLink` component (@/components/BackLink) instead.
#   - Submit flow: JoaBooks uses `useMutation`; JoaOffice uses a plain
#     async `onSubmit` + local `submitting` state instead (no functional
#     difference, just a style choice already established elsewhere in
#     JoaOffice's recurring routes — keep it that way on future pulls).
#   - `source_app` literal: "joabooks" vs "joaoffice" in the createRecurring
#     call, same pattern as recurring-v2.functions.ts's APP_CODE line.
# The FIELD_LABELS map, humanizeError(), the `Search` type + validateSearch
# (deep-link prefill from name/direction/party_id/amount/currency_code/
# description/from/from_id), and the initial-value wiring must stay
# byte-for-byte identical — these were the actual gap found 2026-07-11
# (JoaOffice's copy had silently dropped deep-link prefill support and
# humanized Zod error messages entirely).
src/routes/app.recurring.new.tsx

# NOTE on files intentionally NOT in this manifest:
#   - app.recurring.index.tsx: JoaOffice's list page has diverged
#     deliberately (pagination, SavedViewsBar, Clear-filters chip,
#     admin-gated "New" button, card layout vs JoaBooks' table) - this is
#     accepted independent UI evolution, not drift to reconcile. Do not
#     force this one into the strict pull-from-core pattern.
#   - app.recurring.$id.tsx / $id.index.tsx / $id.edit.tsx: JoaBooks split
#     view+edit into separate routes (with a pathless `$id.tsx` Outlet
#     wrapper); JoaOffice keeps a single `$id.tsx` with an inline edit-mode
#     toggle. Confirmed 2026-07-11 the *feature* surface (mutations,
#     i18n keys) is equivalent between the two structures - this is a
#     routing-structure choice, not a content gap. Re-verify feature
#     parity (not file identity) on future JoaBooks changes to its detail
#     page: diff the exported mutation/function names on both sides
#     rather than the raw file text.
#   - app.settings.recurring.tsx: JoaBooks-only. This is the *legacy*
#     `recurring_schedules` feature (@/lib/recurring.functions), distinct
#     from Planned Transactions (@/lib/recurring-v2.functions). Explicitly
#     out of scope for JoaOffice per the original migration docs - not a
#     gap.
